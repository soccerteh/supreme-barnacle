<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>E164_Phone__c</fullName>
    <description>This field is needed for HeyMarket to work.</description>
    <externalId>false</externalId>
    <formula>IF( LEN(SUBSTITUTE(

SUBSTITUTE(

SUBSTITUTE(

SUBSTITUTE(

SUBSTITUTE(

 Phone__c  , &quot;(&quot;, &quot;&quot;

), &quot;)&quot;, &quot;&quot;

), &quot; &quot;, &quot;&quot;

), &quot;-&quot;, &quot;&quot;

), &quot;+&quot;, &quot;&quot;

)) == 10 , &#39;1&#39;+SUBSTITUTE(

SUBSTITUTE(

SUBSTITUTE(

SUBSTITUTE(

SUBSTITUTE(

 Phone__c  , &quot;(&quot;, &quot;&quot;

), &quot;)&quot;, &quot;&quot;

), &quot; &quot;, &quot;&quot;

), &quot;-&quot;, &quot;&quot;

), &quot;+&quot;, &quot;&quot;

), SUBSTITUTE(

SUBSTITUTE(

SUBSTITUTE(

SUBSTITUTE(

SUBSTITUTE(

  Phone__c   , &quot;(&quot;, &quot;&quot;

), &quot;)&quot;, &quot;&quot;

), &quot; &quot;, &quot;&quot;

), &quot;-&quot;, &quot;&quot;

), &quot;+&quot;, &quot;&quot;

))</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <inlineHelpText>This is for HeyMarket</inlineHelpText>
    <label>E164 Phone</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
