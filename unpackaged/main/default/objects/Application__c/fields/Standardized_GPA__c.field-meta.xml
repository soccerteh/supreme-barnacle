<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Standardized_GPA__c</fullName>
    <description>GPA standardized to the 4.0 scale</description>
    <externalId>false</externalId>
    <formula>if(
contains(text(GPA_Scale__c),&quot;4&quot;), GPA__c,

if(
or(
contains(text(GPA_Scale__c),&quot;5&quot;),
contains(text(GPA_Scale__c),&quot;6&quot;)
),

GPA__c*4/value(text(GPA_Scale__c)),


if(
and(
ispickval(GPA_Scale__c,&quot;100&quot;),
GPA__c &lt;= 100,
GPA__c &gt;= 93
),
4.0,

if(
and(
ispickval(GPA_Scale__c,&quot;100&quot;),
GPA__c &lt;= 92,
GPA__c &gt;= 90
),
3.7,

if(
and(
ispickval(GPA_Scale__c,&quot;100&quot;),
GPA__c &lt;= 89,
GPA__c &gt;= 87
),
3.3,

if(
and(
ispickval(GPA_Scale__c,&quot;100&quot;),
GPA__c &lt;= 86,
GPA__c &gt;= 83
),
3.0,

if(
and(
ispickval(GPA_Scale__c,&quot;100&quot;),
GPA__c &lt;= 82,
GPA__c &gt;= 80
),
2.7,

if(
and(
ispickval(GPA_Scale__c,&quot;100&quot;),
GPA__c &lt;= 79,
GPA__c &gt;= 77
),
2.3,

if(
and(
ispickval(GPA_Scale__c,&quot;100&quot;),
GPA__c &lt;= 76,
GPA__c &gt;= 73
),
2.0,

if(
and(
ispickval(GPA_Scale__c,&quot;100&quot;),
GPA__c &lt;= 72,
GPA__c &gt;= 70
),
1.7,

if(
and(
ispickval(GPA_Scale__c,&quot;100&quot;),
GPA__c &lt;= 69,
GPA__c &gt;= 67
),
1.3,

if(
and(
ispickval(GPA_Scale__c,&quot;100&quot;),
GPA__c &lt;= 66,
GPA__c &gt;= 65
),
1.0,

if(
and(
ispickval(GPA_Scale__c,&quot;100&quot;),
GPA__c &lt;= 64
),
0.0,

null
)
)
)
)
)
)
)
)
)
)
)
)
)</formula>
    <inlineHelpText>GPA standardized to the 4.0 scale</inlineHelpText>
    <label>Standardized GPA</label>
    <precision>18</precision>
    <required>false</required>
    <scale>2</scale>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Number</type>
    <unique>false</unique>
</CustomField>
