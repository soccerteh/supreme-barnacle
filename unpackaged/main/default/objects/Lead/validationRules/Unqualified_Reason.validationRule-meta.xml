<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Unqualified_Reason</fullName>
    <active>true</active>
    <errorConditionFormula>AND(
$User.Bypass_Validation__c = false,

or(
left(RecordTypeId, 15) = left($Label.RT_Lead_Verto, 15),
left(RecordTypeId, 15) = left($Label.RT_Lead_ChannelEnrollment, 15)
),

ISPICKVAL(Status, &quot;Unqualified&quot;),

ISPICKVAL(Unqualified_Reason__c, &quot;&quot;),

or(
ischanged(Status),
ischanged(Unqualified_Reason__c)
)
)</errorConditionFormula>
    <errorDisplayField>Unqualified_Reason__c</errorDisplayField>
    <errorMessage>Please enter an Unqualified Reason to save this record in the Unqualified status.</errorMessage>
</ValidationRule>
